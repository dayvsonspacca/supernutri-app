interface FormProps {
    title?: String,
    children?: React.ReactNode,
    size?: String
}

interface HeaderProps {
    title: String,
    children?: React.ReactNode,
}

interface CriancaProps {
    nome: String,
    img: String
}

interface JogoProps {
    nome: String,
    bloqueado: Boolean,
}

interface NutrinhoProps {
    nome: String,
    desc: String,
    image: String
}